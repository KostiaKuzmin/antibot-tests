[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
markers =
    positive: real browser scenarios that should be classified as REAL_BROWSER
    negative: bot/headless/antidetect scenarios that should be blocked
    edge: borderline scenarios — SUSPICIOUS verdict or rate-limit triggers
